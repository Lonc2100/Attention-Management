# 实施计划

1. 先写失败测试：界面上下文解析、唯一匹配、跨项目同名消歧、不确定时不沿用旧线程。
2. 实现 Windows UI Automation reader 与超时/错误边界。
3. 重构 tracker：当前界面为主、线程列表为映射、旧项目不再兜底。
4. 更新状态文案、README、后端规范、版本与 v0.4.1 发布说明。
5. 运行 unit/typecheck/build/E2E 两轮；完成打包、smoke、升级安装与自启动验证。
6. 写测试报告，归档 Trellis 任务，提交并推送 main 与 v0.4.1 标签。

## 回滚点

- 实现阶段：删除 reader 注入并恢复 tracker 构造即可回滚，存储无变化。
- 发布阶段：保留 v0.4.0 tag/安装包；若真实界面检测失败，回滚程序不会损坏 v4 数据。
