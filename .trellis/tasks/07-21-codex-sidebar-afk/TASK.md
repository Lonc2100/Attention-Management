# Codex 侧边栏归属与 AFK 默认校准 · 任务主控

## 0. 元信息

- 创建日期：2026-07-21
- 当前批次：Batch 1
- 自主度模式：b（只在真实环境验证或不可逆分叉时停止）
- 当前结论：AFK 校准、Harness-lite 拆分和自动化验证完成，并已推送提交 `fd37cf6`；Codex 侧边栏实时识别被当前桌面版的外部可访问性信号阻塞。未创建发布标签，未升级正式安装。

## 1. 用户任务

- 修复 Codex 项目侧边栏中的项目识别，作为 Harness-lite 第一次架构拆分；随后将 AFK 默认阈值调整为贴合“阅读不超过三分钟”的个人规律，并验证真实数据。

## 2. 已确认边界

- 不读取键盘正文、不录屏、不截图、不点击或控制 Codex。
- 不猜测归属；不唯一则待分类。
- 保留已有用户的 AFK 设置。

## 3. 任务队列

| # | 任务 | 状态 | 依赖 | 预期产出 |
| --- | --- | --- | --- | --- |
| T1 | 记录真实 Codex 可见上下文基线 | ✅ | - | `visible === null` 的可复现 integration 结果 |
| T2 | Provider + Service 首次抽取 | ✅ | T1 | Harness 合规的归属链与兼容 Tracker |
| T3 | 侧边栏选择与匹配测试 | ✅ / 外部阻塞 | T2 | 正例、歧义降级测试；真实 UI 无可读节点 |
| T4 | AFK 新用户默认改为 5 分钟 | ✅ | - | 兼容性测试、文案与无窗口残留纠错 |
| T5 | 自动化、E2E 与真实数据验证 | ✅ / 外部阻塞 | T2,T3,T4 | 87 单测、17 E2E、包 smoke；侧边栏实测失败有证据 |
| T6 | Trellis 归档、提交和推送 | ✅（发布阻塞保留） | T5 | 已提交并推送；不打发布标签、不升级正式安装 |

## 6. 触发用户介入条件

1. 真实 Codex Desktop 不暴露足够的侧边栏可访问性信息。
2. 可见标题无法与 app-server 线程唯一匹配，且没有可靠的额外只读标识。
3. 真实 AFK 验证需要用户实际离开电脑并返回。
