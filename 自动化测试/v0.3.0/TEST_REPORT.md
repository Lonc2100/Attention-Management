# v0.3.0 测试报告

状态：通过

## 基线

- `npm test`：通过（19 passed，2 skipped）
- `npm run typecheck`：通过
- `npm run build`：通过

## 本版结果

- `npm test`：26 passed，2 skipped；跳过项为需要显式启用的真实 Codex 集成测试。
- `npm run typecheck`：通过。
- `npm run build`：通过。
- `npm run test:e2e`：连续多轮通过，最终轮 11 项全部通过。
- 双分辨率：1440×900、1600×1000 均无横向溢出，截图人工审查通过。
- 悬浮窗：独立窗口、展开、隐藏、设置恢复、桌面模式、置顶恢复、不抢焦点均通过。
- Windows NSIS：`TimeEfficiency-0.3.0-Setup-x64.exe` 构建成功。
- 打包应用冒烟：通过，内置 ActivityWatch v0.13.2 与诊断链路可用。
- 已安装应用：v0.3.0 静默升级成功，自启动注册项关闭/恢复测试通过。
- SHA-256：`666CDF4D49E0EFBFFD9724FB15D3EBAFE023115D2E32E95411BB4DF3DFBC5C9D`。

## 人工与视觉验收

- H-01：通过。两个目标分辨率布局层级清楚，无横向溢出。
- H-02：通过。置顶/桌面模式和非激活显示由 Windows E2E 验证，展开态截图审查通过。
- H-03：部分通过。无效 displayId 的主屏回退由 3 个单元测试覆盖；本轮没有真实拔插第二块物理显示器。
- H-04：通过核心项。安装后 ActivityWatch、主窗口、悬浮窗和登录自启链路可运行；托盘菜单沿用既有实现，本轮未自动点击系统托盘菜单。

## 缺陷结论

- P0：0
- P1：0
- 发布阻塞项：0

## 已知边界

- 真实多显示器拔插需要在具备第二块屏幕时人工验证；自动化负责覆盖无效 displayId 的回退逻辑。
- Windows 安装包当前没有产品专用图标与商业代码签名；属于后续对外产品化事项，不影响本次自用版功能验收。
